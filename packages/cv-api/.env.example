NODE_ENV=development
PORT=3100

# Postgres (Supabase / Neon) — connection string en mode "session pooler" recommandé
DATABASE_URL=postgres://user:password@host:5432/postgres

# Origine(s) autorisée(s) pour CORS (séparées par des virgules).
# Défaut : ports Angular de dev courants (4200 et 4400).
CORS_ORIGIN=http://localhost:4200,http://localhost:4400

# Optionnel : origines supplémentaires par expression régulière
# (ex. canaux de prévisualisation Firebase Hosting).
# CORS_ORIGIN_REGEX=^https://mon-cv--preview-.*\.web\.app$

# Jeton d'administration (24 caractères minimum) exigé sur toute écriture
# (POST / PATCH / PUT / DELETE) via `Authorization: Bearer <token>`.
# Les lectures (GET) restent publiques : le site CV n'a besoin d'aucun secret.
# Générer par exemple avec : openssl rand -hex 32
ADMIN_TOKEN=change-moi-par-un-secret-aleatoire-de-32-octets
