import React from 'react';
import { View, Text } from 'react-native';

/* eslint-disable-next-line */
export interface MobileDsProps {}

export function MobileDs(props: MobileDsProps) {
  return (
    <View>
      <Text>Welcome to mobile-ds!</Text>
    </View>
  );
}

export default MobileDs;
