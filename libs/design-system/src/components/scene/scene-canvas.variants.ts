import { cva } from 'class-variance-authority';

export const sceneCanvasVariants = cva(
  'border-border from-muted to-background relative block w-full overflow-hidden rounded-xl border bg-gradient-to-b',
);
